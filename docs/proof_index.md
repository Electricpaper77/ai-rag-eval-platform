# Proof Index

AgentTrust IQ is a model-agnostic AI agent reliability and evaluation platform that tests groundedness, citation support, prompt-injection resistance, PII handling, unsafe tool actions, latency, and auditability before release.

| Proof item | File path | What it proves |
| --- | --- | --- |
| Canonical aggregate metrics | `docs/artifacts/eval_runs/hiring_eval_summary.json` | Six-case controlled summary used by recruiter-facing materials |
| Underlying JSONL cases | `docs/artifacts/eval_runs/hiring_eval.jsonl` | Six case rows and matching summary row |
| Deterministic demo contract | `tests/test_agenttrust_demo.py` | Score 92 fixture, checks, tool tiers, and JSONL output |
| Hiring artifact contract | `tests/test_eval_harness.py` | Summary and case-artifact validation |
| Optional Gemini replay | `docs/artifacts/xprize/judge_replay_latest.jsonl` | Deterministic replay with explicit Gemini-disabled status when no key is configured |
| Prometheus evidence | `docs/artifacts/metrics_sample.txt` | Prometheus-format metrics |
| Deployment packaging | `Dockerfile`, `k8s/`, `helm/`, `infra/terraform/` | Packaging only; no active production cluster is claimed |

Canonical source: `docs/artifacts/eval_runs/hiring_eval_summary.json`, generated by `app.eval_harness.run_eval_harness` from the checked-in JSONL cases. Sample size: **6 deterministic fixture cases**. The **0.159 ms** value is local deterministic evaluator runtime, not LLM, API, RAG, or end-to-end latency. The canonical metrics are controlled demo/test results, not customer-production telemetry. The pytest suite was not executed in this environment. Gemini is optional and is not part of the deterministic baseline.
