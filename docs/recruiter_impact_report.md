# Recruiter-Impact Report

AgentTrust IQ is a model-agnostic AI agent reliability and evaluation platform that tests groundedness, citation support, prompt-injection resistance, PII handling, unsafe tool actions, latency, and auditability before release.

## Senior NVIDIA AI Infrastructure Review

Single highest-impact missing infrastructure feature: collector-compatible OpenTelemetry trace export.

## Why This Feature

The repository already had the other prioritized hiring signals:

- TTFT metrics
- tokens/sec metrics
- backend comparison leaderboard
- Kubernetes deployment manifests

The missing gap was that traces existed only as local OTEL-shaped JSONL artifacts. For infrastructure roles, an OTLP-compatible export path is a stronger engineering signal because it connects gateway requests, routing decisions, and backend calls to a standard observability pipeline.

## Implemented

- Optional OpenTelemetry SDK integration in `app/tracing.py`.
- OTLP HTTP trace export when `OTEL_EXPORTER_OTLP_ENDPOINT` or `OTEL_EXPORTER_OTLP_TRACES_ENDPOINT` is configured.
- OpenTelemetry Collector service in `docker-compose.yml`.
- Collector config in `observability/otel-collector/config.yaml`.
- Kubernetes environment wiring for OTLP export in `k8s/deployment.yaml`.
- Local JSONL fallback preserved for offline tests and portfolio proof.
- Pipeline proof artifact at `docs/artifacts/opentelemetry_pipeline.json`.

## Hiring Signal Raised

Before: trace-like JSONL artifacts showed local request/backend causality.

After: the gateway has a collector-compatible trace export path while still generating offline proof artifacts for recruiters. No hosted collector deployment is claimed.

## Validation

- Focused validation: `tests/test_agenttrust_demo.py` and `tests/test_eval_harness.py`
- `docker compose config --quiet`: valid, with a local Docker config permission warning unrelated to the repository config.
- New proof artifacts:
  - `docs/artifacts/opentelemetry_pipeline.json`
  - `docs/artifacts/otel_traces.jsonl`
  - `observability/otel-collector/config.yaml`

Canonical source: `docs/artifacts/eval_runs/hiring_eval_summary.json`, generated by `app.eval_harness.run_eval_harness` from the checked-in JSONL cases. Sample size: **6 deterministic fixture cases**. The **0.159 ms** value is local deterministic evaluator runtime, not LLM, API, RAG, or end-to-end latency. Recruiter-facing metrics are controlled demo/test results, not customer-production telemetry. The pytest suite was not executed in this environment. Gemini is optional and is not required for this observability path.

