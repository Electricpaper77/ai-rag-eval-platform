apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: ai-eval-inference-pdb
  labels:
    app: ai-eval-inference
    workload-type: inference
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: ai-eval-inference
