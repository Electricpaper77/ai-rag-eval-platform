# AgentTrust IQ

AgentTrust IQ is a model-agnostic AI agent reliability and evaluation platform that tests groundedness, citation support, prompt-injection resistance, PII handling, unsafe tool actions, latency, and auditability before release.

[Open the AgentTrust IQ Command Center](https://ai-agent-reliability-platform-rtcd.vercel.app/agenttrust-iq-command-center.html)

## What It Does

AgentTrust IQ turns reliability checks into an explicit `release`, `block`, or `escalate` decision with replayable JSONL evidence. The default workflow is deterministic and local, so reviewers can reproduce it without a paid API or external service.

- Checks answers against retrieved evidence and citations.
- Detects prompt-injection, PII, hallucination, and unsafe-action risks.
- Records evaluator latency and structured audit evidence.
- Exposes a FastAPI demo endpoint and recruiter-facing static walkthrough.
- Supports Gemini as an optional model-assisted evaluator when `GEMINI_API_KEY` is configured; Gemini is not required for the baseline workflow.

## Canonical Evidence

All aggregate metrics in recruiter-facing materials use one source: [`docs/artifacts/eval_runs/hiring_eval_summary.json`](docs/artifacts/eval_runs/hiring_eval_summary.json). Its underlying case records are in [`docs/artifacts/eval_runs/hiring_eval.jsonl`](docs/artifacts/eval_runs/hiring_eval.jsonl).

| Metric | Controlled result | Qualification |
| --- | ---: | --- |
| Cases passed | **6 / 6** | Deterministic fixtures |
| Eval pass rate | **100.0%** | Six-case smoke run |
| Hallucination rate | **0.0%** | Rule-based fixture checks |
| Citation precision | **100.0%** | Citation-required fixtures |
| Refusal accuracy | **100.0%** | Unsafe-request and prompt-injection fixtures |
| Evaluator p95 latency | **0.159 ms** | Local scoring time, not agent/model or network latency |

Canonical source: `docs/artifacts/eval_runs/hiring_eval_summary.json`, generated by `app.eval_harness.run_eval_harness` from the checked-in JSONL cases. Sample size: **6 deterministic fixture cases**. The **0.159 ms** value is local deterministic evaluator runtime, not LLM, API, RAG, or end-to-end latency. These are controlled demo/test results, not customer-production telemetry, production capacity claims, independent benchmarks, or paid-provider performance. The pytest suite was not executed in this environment.

The Command Center also shows an **Agent Readiness Score of 92** for one fixed deterministic demo fixture. That score is a fixture result asserted by [`tests/test_agenttrust_demo.py`](tests/test_agenttrust_demo.py), not an aggregate benchmark or customer metric.

## Run Locally

## Verify AgentTrust IQ in 5 Minutes

Install the lightweight local dependencies:

```bash
python -m pip install -r requirements-lite.txt
```

Create the deterministic 50-case evidence pack (the requested `evaluations/` path is a stable alias for the checked-in NVIDIA-shaped fixture suite):

```bash
python -m app.eval.run --provider mock --suite evaluations/nvidia_eval_pack.jsonl --output artifacts/latest
```

The output is explicitly labeled **mock evidence**. It proves the deterministic evaluator and artifact contract, not NVIDIA/GPU, model, API, RAG, network, or production performance.

Run the focused verification:

```bash
python -m pytest tests/test_recruiter_evidence.py tests/test_nvidia_eval_pack.py -q
```

Open the dashboard:

```bash
python -m uvicorn backend.app.main:app --host 127.0.0.1 --port 8000
```

Then visit [http://127.0.0.1:8000/recruiter-evidence](http://127.0.0.1:8000/recruiter-evidence). Downloadable evidence is in `artifacts/latest/summary.json` and `artifacts/latest/results.jsonl` (or through the page links).

Validation note (2026-07-24): the focused evidence tests passed (`8 passed`). The complete suite does **not** currently pass: the committed main baseline reproduces `104 passed, 28 failed, 1 xfailed`; those failures are legacy API, dashboard, GPU, routing, and observability contract mismatches, not evidence-pack coverage. Do not represent the full suite as green until those contracts are reconciled.

Start the deterministic API:

```bash
python -m uvicorn backend.app.main:app --host 127.0.0.1 --port 8000
```

Request the reliability workflow:

```bash
curl -s http://127.0.0.1:8000/demo/agenttrust-iq
```

Run the focused validation:

```bash
python -m pytest tests/test_agenttrust_demo.py -q
python -m pytest tests/test_eval_harness.py -q
```

Replay the deterministic audit trace:

```bash
python scripts/judge_replay.py
```

Optionally include Gemini-assisted evaluation:

```bash
GEMINI_API_KEY=your_api_key python scripts/judge_replay.py
```

The deterministic evaluator always runs and remains the fallback if Gemini is unconfigured or unavailable.

## Evidence Map

| Evidence | Purpose |
| --- | --- |
| [`PROOF.md`](PROOF.md) | Short reviewer verification path |
| [`docs/artifacts/eval_runs/hiring_eval_summary.json`](docs/artifacts/eval_runs/hiring_eval_summary.json) | Canonical recruiter-facing aggregate metrics |
| [`docs/artifacts/eval_runs/hiring_eval.jsonl`](docs/artifacts/eval_runs/hiring_eval.jsonl) | Six controlled case records plus summary row |
| [`docs/artifacts/xprize/judge_replay_latest.jsonl`](docs/artifacts/xprize/judge_replay_latest.jsonl) | Deterministic replay output; records Gemini as disabled when no key is set |
| [`tests/test_agenttrust_demo.py`](tests/test_agenttrust_demo.py) | Demo response, score, checks, tool tiers, and JSONL contract |
| [`tests/test_eval_harness.py`](tests/test_eval_harness.py) | Hiring evidence artifact contract |

## Implemented Scope

- FastAPI evaluation and OpenAI-compatible inference surfaces.
- Deterministic groundedness, citation, hallucination, refusal, PII, and prompt-injection checks.
- Unsafe tool-action tiers with human-approval escalation.
- JSONL audit records and Prometheus-format metrics.
- Optional Gemini evaluator integration through `GEMINI_API_KEY`.
- Docker, Kubernetes, Helm, and Terraform packaging; no live production cluster is claimed.

## Project Links

- [Static project walkthrough](https://ai-agent-reliability-platform-rtcd.vercel.app/)
- [AgentTrust IQ Command Center](https://ai-agent-reliability-platform-rtcd.vercel.app/agenttrust-iq-command-center.html)
- [GitHub repository](https://github.com/Electricpaper77/ai-rag-eval-platform)
- [Proof guide](PROOF.md)

## Resume-Safe Claim

Built AgentTrust IQ, a model-agnostic FastAPI agent reliability and evaluation platform with deterministic groundedness, citation, hallucination, refusal, prompt-injection, unsafe-action, and PII checks; produced replayable JSONL audit evidence and an explicit release, block, or escalate decision, with Gemini available only as an optional evaluator integration.
